

/*
DETALHE      || Função integrada à biblioteca AdaFruit
DETALHE      || Função que acompanhar objetos do tipo "AF_DCMotor"
OBJETIVO     || Definir a DIREÇÃO de rotação de um motor DC (frente, trás, parado)
IMPORTAÇÃO   || #include <AFMotor.h>
INSTANCIAÇÃO || AF_DCMotor nomeObjeto(id_do_motor);
USO          || nomeObjeto.run(FORWARD, BACKWARD, RELEASE)
*/
